server {
        listen {{ lighthouse_port }} default_server;
        listen [::]:{{ lighthouse_port }} default_server;
        root /usr/share/nginx/lighthouse-master;
        index index.html index.htm index.nginx-debian.html;
        server_name _;
        location / {
                try_files $uri $uri/ =404;
        }
}